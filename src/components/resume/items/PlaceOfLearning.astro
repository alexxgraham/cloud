---
import EduSpan from './_Span.astro';
const { href, place, title, date, location } = Astro.props;
---
<div class="flex flex-col">
    <EduSpan href={href} title={place} subtitle={title} />
    <p class="italic text-xs">{date}</p>
    <p class="text-xs">{location}</p>
</div>